# Set the default tmux shell to The users shell
set-option -g default-shell $SHELL

#-------------------------------------------------------#
#change prefix from C-b to C-a

unbind C-b
unbind C-a
set -g prefix C-a
